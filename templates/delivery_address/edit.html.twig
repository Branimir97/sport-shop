{% extends 'base.html.twig' %}

{% block title %}Adresa "{{ delivery_address.street }}"{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset("css/container50.css") }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-2 bg-light p-2">Adresa "{{ delivery_address.street }}"</h2>
        {% if is_granted("ROLE_ADMIN") %}
            <a href="{{ path('delivery_address_index') }}">
                <i class="fas fa-arrow-alt-circle-left"></i>
                Lista adresa isporuka
            </a>
            {% else %}
            <a href="{{ path('account_settings') }}">
                <i class="fas fa-arrow-alt-circle-left"></i>
                Postavke računa
            </a>
        {% endif %}
        {{ include('delivery_address/_form.html.twig', {'button_label': 'Spremi promjene'}) }}
        <div class="text-center">
            {{ include('delivery_address/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
