{% extends 'base.html.twig' %}

{% block title %}
    {{ 'error403.title'|trans({}, 'error') }}
{% endblock %}
{% block body %}
    <div class="container text-center">
        <h1 class="bg-light">
            {{ 'error403.title'|trans({}, 'error') }}
        </h1>
        <img src="{{ asset('images/logo.png') }}" alt="sport_shop_icon">
        <p>
            {{ 'error403.message'|trans({}, 'error') }}
            <a href="{{ path('home') }}">
                {{ 'homepage_link'|trans({}, 'error') }}
            </a>
        </p>
    </div>
{% endblock %}