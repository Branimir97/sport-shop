{% extends 'base.html.twig' %}

{% block title %}Kategorija {{ category.name }}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset("css/container.css") }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center p-2 mt-2 bg-light">Uređivanje podataka o kategoriji</h2>

        {{ include('category/_form.html.twig', {'button_label': 'Spremi promjene'}) }}

        <a href="{{ path('category_index') }}">
            <i class="fas fa-arrow-alt-circle-left"></i>
            Nazad na listu kategorija
        </a>

        {{ include('category/_delete_form.html.twig') }}
    </div>
{% endblock %}
