{{ form_start(form) }}
    {{ form_row(form.value) }}
<div class="text-center">
    <button class="btn btn-outline-secondary mb-3">{{ button_label|default('Dodaj boju') }}</button>
</div>{{ form_end(form) }}
