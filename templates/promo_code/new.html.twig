{% extends 'base.html.twig' %}

{% block title %}
    {{ 'new.title'|trans({}, 'promo_code') }}
{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset("css/container50.css") }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-2 bg-light p-2">
            {{ 'new.heading'|trans({}, 'promo_code') }}
        </h2>
        <a href="{{ path('promo_code_index') }}">
            <i class="fas fa-arrow-alt-circle-left"></i>
            {{ 'promoCodesList'|trans({}, 'promo_code') }}
        </a>
        {{ include('promo_code/_form.html.twig') }}
    </div>
{% endblock %}
