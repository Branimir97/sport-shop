{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center">
        <button class="btn btn-success mb-3">
            {{ button_label|default('form.button_label'|trans({}, 'loyalty_card')) }}
        </button>
    </div>
{{ form_end(form) }}
