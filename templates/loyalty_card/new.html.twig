{% extends 'base.html.twig' %}

{% block title %}
    {{ 'new.title'|trans({}, 'loyalty_card') }}
{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset("css/container50.css") }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-2 bg-light p-2">
            {{ 'new.heading'|trans({}, 'loyalty_card') }}
        </h2>
        <a href="{{ path('loyalty_card_index') }}">
            <i class="fas fa-arrow-alt-circle-left"></i>
            {{ 'loyaltyCardList'|trans({}, 'loyalty_card') }}
        </a>
        {{ include('loyalty_card/_form.html.twig') }}
    </div>
{% endblock %}
