{% extends 'base.html.twig' %}

{% block title %}
    {{ 'new.title'|trans({}, 'manufacturer') }}
{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset("css/container50.css") }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-2 bg-light p-2">
            {{ 'new.heading'|trans({}, 'manufacturer') }}
        </h2>
        <a href="{{ path('manufacturer_index') }}">
            <i class="fas fa-arrow-alt-circle-left"></i>
            {{ 'manufacturerList'|trans({}, 'manufacturer') }}
        </a>
        {{ include('manufacturer/_form.html.twig') }}
    </div>
{% endblock %}
