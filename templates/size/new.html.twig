{% extends 'base.html.twig' %}

{% block title %}
    {{ 'new.title'|trans({}, 'size') }}
{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/container50.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-2 bg-light p-2">
            {{ 'new.heading'|trans({}, 'size') }}
        </h2>
        <a href="{{ path('size_index') }}">
            <i class="fas fa-arrow-alt-circle-left"></i>
            {{ 'sizesList'|trans({}, 'size') }}
        </a>
        {{ include('size/_form.html.twig') }}
    </div>
{% endblock %}
