{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center">
        <button class="btn btn-success mb-3">
            Spremi promjene
        </button>
    </div>
{{ form_end(form) }}
