{% extends 'base.html.twig' %}

{% block title %}
    {{ 'edit_size_quantity.title'|trans({}, 'item') }}
    - [{{ itemSize.size.value }}]
{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/container40.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-2 bg-light p-2">
            {{ 'edit_size_quantity.title'|trans({}, 'item') }}
            - [{{ itemSize.size.value }}]
        </h2>
        <a href="{{ path('item_edit', {'id':itemSize.item.id}) }}">
            <i class="fas fa-arrow-alt-circle-left"></i>
            {{ 'edit_size_quantity.editItem'|trans({}, 'item') }}
        </a>
        {{ include('item/edit_size_quantity_form.html.twig') }}
    </div>
{% endblock %}
