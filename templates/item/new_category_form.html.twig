{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center">
        <button class="btn btn-success mb-3">
            {{ 'form_add_category.button_label'|trans({}, 'item') }}
        </button>
    </div>
{{ form_end(form) }}
