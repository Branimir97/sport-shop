{{ form_start(formNewItem) }}
    {{ form_widget(formNewItem) }}
    <div class="text-center">
        <button class="btn btn-success mb-3">{{ button_label|default('Dodaj artikl') }}</button>
    </div>
{{ form_end(formNewItem) }}
