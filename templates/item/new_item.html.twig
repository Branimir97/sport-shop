{% extends 'base.html.twig' %}

{% block title %}
    {{ 'new.title'|trans({}, 'item') }}
{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/container50.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-2 bg-light p-2">
            {{ 'new.heading'|trans({}, 'item') }}
        </h2>
        <a href="{{ path('item_index') }}">
            <i class="fas fa-arrow-alt-circle-left"></i>
            {{ 'itemsList'|trans({}, 'item') }}
        </a>
        {{ include('item/new_item_form.html.twig') }}
    </div>
{% endblock %}
